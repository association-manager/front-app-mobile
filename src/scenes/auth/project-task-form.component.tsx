import React from 'react';
import ContentView from '../../scenes/project-tasks/task-edit-form';

export const ProjectTaskEditFormScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
