import React from 'react';
import ContentView from '../layouts/login-system/user-login-page';

export const UserLoginScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
