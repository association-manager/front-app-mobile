import React from 'react';
import ContentView from '../layouts/login-system/user-forgot-password';

export const UserForgotPasswordScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
